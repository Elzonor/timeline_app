<%# Layout per timeline vuota %>
<div class="empty-state-container">
  <div class="empty-state-content">
    <div class="empty-state-text">
      <h2>Ancora nessun evento per questa timeline...</h2>
      <p>...crea il primo evento per iniziare a costruire la tua timeline 🤩</p>
    </div>
    <div class="empty-state-action">
      <%= link_to new_timeline_event_path(@timeline), class: 'btn btn-primary text-white text-nowrap' do %>
        <i class="fas fa-plus"></i> <%= t('actions.add_event') %>
      <% end %>
    </div>
  </div>
</div> 